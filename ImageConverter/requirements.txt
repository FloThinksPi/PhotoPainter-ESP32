# ESP32 Photo Frame Image Converter Dependencies
# Install with: pip install -r requirements.txt

# Core image processing library
Pillow>=10.0.0
pillow-heif>=0.10.0    # HEIF/HEIC support (iPhone photos)
pillow-avif-plugin>=1.4.0  # AVIF support (modern web format)